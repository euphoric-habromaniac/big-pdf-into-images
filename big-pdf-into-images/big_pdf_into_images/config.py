# Default format for saving images. Can be 'png', 'jpeg', or 'jpg'
DEFAULT_FORMAT = 'png'  

# Default DPI (dots per inch) for the conversion, higher means better quality
DEFAULT_DPI = 300  

# Default output directory to store the converted images
DEFAULT_OUTPUT_DIR = 'output'  
